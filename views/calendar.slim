css:
  .legends a {
    display: inline-block;
    margin: 10px 10px 0 0;
    color: black;
    text-decoration: none;
    font-weight: bold;
    padding: 5px 10px;
    border-radius: 6px;
  }
  .fc-toolbar h2 {
    font-size: 20px;
  }

javascript:
  setTimeout("location.reload();",30000);
  var focused_booking = {};
  $(document).ready(function() {
    
    $('#calendar').fullCalendar({
      defaultView: 'agendaWeek',
      firstDay: 1, // Mon
      // editable: true,
      allDaySlot: false,
      height: 500,
      scrollTime: '09:00:00',
      minTime: '08:00:00',
      maxTime: '21:00:00',
      eventSources: [
        '/api/v1/upcoming'
      ],
      businessHours: {
          dow: [ 1, 2, 3, 4, 5 ], // Mon-Fri
          start: '8:00',
          end: '20:00',
      },
      eventClick: function(calEvent, jsEvent, view) {
        // Show popup with booking details
        focused_booking = calEvent;
        $('#bookingModal').modal();
        $('#bookingModal .modal-title').text(calEvent.purpose + " @ " + calEvent.space);
        $('#bookingModal .modal-body').html("booked by <b>" + calEvent.username + "</b><br>From : " + calEvent.start + "<br>To : " +  calEvent.end);
        if (calEvent.uid == #{session[:uid]}) {
          $("#bookingModal button#remove").show();
        } else {
          $("#bookingModal button#remove").hide();
        }
      },
      eventDragStop: function(calEvent, jsEvent, ui, view) {
        console.log(calEvent);
      },
      eventResize: function(calEvent, delta, revertFunc, jsEvent, ui, view) {
        console.log(calEvent);
      }
    });
  });

  function removeBooking() {
    sure = confirm("Are you sure you want to remove this booking?");
    if (!sure) return;

    $.ajax({
      url: '/api/v1/bookings/' + focused_booking.id,
      type: 'DELETE',
      success: function(result) {
        $('#bookingModal').modal('hide');
        $('#calendar').fullCalendar("refetchEvents");
      }
    });
  }

#calendar

.legends
  a Legends
  - Space.all.each do |s|
    a style="background-color:#{s.color}"= s.name

#bookingModal.modal.fade role="dialog" tabindex="-1"
  .modal-dialog.modal-sm role="document"
    .modal-content
      .modal-header
        button.close aria-label="Close" data-dismiss="modal" type="button"
          span aria-hidden="true" &times;
        h4.modal-title#bookingTitle Title
      .modal-body
        p Content
      .modal-footer
        button.btn.btn-danger#remove type="button" onclick="javascript:removeBooking();" Remove Booking
        button.btn.btn-default data-dismiss="modal" type="button" Close
